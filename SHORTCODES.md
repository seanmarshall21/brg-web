# BRG — Shortcode Reference

> GENERATED from `kit/registry.json` by `kit/build.py`. Edit the registry, then rerun. Do not edit this file by hand.

Pages/sections are code fragments on Netlify pulled natively into WordPress; edit a fragment → `git push` → live in ~60s. The nav is a WordPress menu (Appearance → Menus).

## Shortcodes

| Shortcode | Type | Attributes |
|---|---|---|
| `[brg_<slug>]` | embed | `ttl`, `chrome` |
| `[brg page="…"]` | embed | `page`, `ttl`, `chrome` |
| `[brg_<id>]` | embed | `ttl`, `anchor`, `slots` |
| `[brg_section id="…"]` | embed | `id`, `ttl`, `anchor` |
| `[brg_nav]` | php | `layout`, `left`, `right`, `sticky`, `bg`, `bgcolor`, `opacity`, `ttl` |
| `[brg_footer]` | php | `ttl` |
| `[vc_embed url="…"]` | php | `url`, `ttl` |

## Attributes by shortcode

### `[brg_<slug>]` — A whole page fragment (v2.0 monolith) + shared header/footer.  ·  v1 `88180b5dc4cf`
Aliases: brg_home, brg_our-restaurants, brg_team, brg_community, brg_careers (from pages.json)

| Attr | Default | Does |
|---|---|---|
| `ttl` | `120` | Server cache seconds. ttl="0" = always fresh. |
| `chrome` (on / 0) | `on` | chrome="0" renders the page bare — no shared header/footer. |

### `[brg page="…"]` — Generic form of the page shortcode.  ·  v1 `50710ec86127`

| Attr | Default | Does |
|---|---|---|
| `page` | `home` | Which page fragment (generic form of the aliases). |
| `ttl` | `120` | Server cache seconds. |
| `chrome` (on / 0) | `on` | chrome="0" = no header/footer. |

### `[brg_<id>]` — One section fragment (no chrome). Stack these to build a page.  ·  v1 `22483f6c6833`
Aliases: brg_community-hero, brg_careers-apply, brg_home-values, … (from sections.json)

| Attr | Default | Does |
|---|---|---|
| `ttl` | `120` | Server cache seconds. |
| `anchor` | `—` | Injects id="…" on the section root for jump links (/careers/#apply). |
| `slots` | `—` | Per-section {{token}} overrides — none declared yet; appears when a section adds them. |

### `[brg_section id="…"]` — Generic form of the section shortcode.  ·  v1 `9d639f65bd4c`

| Attr | Default | Does |
|---|---|---|
| `id` | `—` | Which section (generic form). e.g. id="cta-band". |
| `ttl` | `120` | Server cache seconds. |
| `anchor` | `—` | Jump-link id on the section root. |

### `[brg_nav]` — WordPress-menu-driven nav (fixed). Content from Appearance → Menus.  ·  v1 `e4a3ae5fc13b`

| Attr | Default | Does |
|---|---|---|
| `layout` (left / split / center / compact) | `left` | center = logo centered; compact = logo-left, overflow → More drawer. |
| `left` | `2` | Items per side (split/center) or total-before-overflow (compact). |
| `right` | `2` | Items on the right side (split/center). |
| `sticky` (pin / hide) | `pin` | pin stays; hide = hide-on-scroll-down (nav is fixed, so it always sticks). |
| `bg` (solid / none / frost) | `solid` | solid (white bar, reserves space) · none (transparent, floats over hero) · frost (dark frosted glass + blur). |
| `bgcolor` | `#fff` | Background color for solid/frost, any CSS color (bgcolor="#131210"). |
| `opacity` | `1` | Background opacity 0–1 (solid/frost). |
| `ttl` | `120` | Server cache seconds. |

### `[brg_footer]` — The shared BLACKTOP lockup footer.  ·  v1 `2246471ccc36`

| Attr | Default | Does |
|---|---|---|
| `ttl` | `120` | Server cache seconds. |

### `[vc_embed url="…"]` — One-off: inline any Netlify fragment by full URL.  ·  v1 `9bc191093a53`

| Attr | Default | Does |
|---|---|---|
| `url` | `—` | Full URL of any Netlify fragment. Netlify-host only. |
| `ttl` | `120` | Server cache seconds. |

## Global

- `ttl` (default `120`) — Server cache seconds on any shortcode. ttl="0" = always fresh (test pages).
- `?brg_refresh=1` (default `-`) — URL param (not an attr) — forces every BRG shortcode on the page to re-fetch once.

_registry v1 · built registry-generated_
