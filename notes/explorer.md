# Notes — EXPLORER (research / ideas / experiments)

**Single writer: the Explorer chat only.** Others READ this; they write their own `notes/*.md`.
Research direction/sitemap/content/best-practices and propose sections & features as **SPECS —
don't ship production code.** Log proposals as `PLAN:` for the Controller to approve (`DECISION:` in
`notes/controller.md`); the Finesser builds approved ones.
Newest first. Format: `TYPE: <YYYY-MM-DD> · note`  (TYPE = PLAN / DONE / NEED / DECISION / QUESTION).
Long-form specs live in **[`notes/explorer/`](explorer/)** — this file stays a scannable log.

- DONE: 2026-08-13 · **SPEC-008 ruled on, and Conti caught a field name I had wrong.** He verified §1 in the files rather than taking it — 12 is the brand's count, the stat presents it as the group's, and our own copy contains the correction. Outcomes: **delete-not-sync approved as architecture** (*one fact, one home* — the same rule as one-owner-per-file, applied to facts), **but the rewrite is copy so the wording is Sean's gate, not Conti's**; **bidirectional `doc` cross-references approved and sent to Finn** (editor guidance, invisible to visitors, so integration rather than design); **the shared-facts `--check` ruled OUT, as a decision and not a deferral.** That last one was my proposal and he has the better of it: it could only ever compare wiring-day defaults, so its green would read as *the numbers agree* — the strongest claim it structurally cannot make, and a misread green is the exact failure this spec is about. **My error: I put the location count in `home-community` · `body_1`; it's `body_2`.** `body_1` is a different paragraph, so the §4 rewrite applied there would have overwritten the wrong copy and left the count standing. Fixed at every occurrence — and the §3 ask still names `body_1` deliberately, since that's the slot directly beneath the literal paragraph an editor can't see.
- PLAN: 2026-08-13 · **SPEC-008 — [what else assumed the copy was ours](explorer/editable-copy-audit.md).** Conti named the category approving my `.ln` fix — *making something editable promotes every latent assumption about that content into a live risk* — so I audited the 16 `slots.json` files and 18 fragments for the rest. **Three findings, and the first is already wrong on the live site.** (1) **The location count lives in three editable places and they disagree today.** `home-community` is explicit that 12 counts **Board & Brew only** and Odie's is additional; `community-stats` presents the same 12 as **BRG's** footprint, which by the site's own arithmetic is **13**. So the one figure on that page I called "already true" in SPEC-007 §2 — the one that was never `XX`, and therefore the one Sean is least likely to check — **is either understated or mislabelled, and I was wrong to wave it through.** (2) The canonical `odies_name` field says `Odie's Pizza` while its own file's prose says `Odie's Pizza Co.` (3) `home-community` is **half-editable** — paragraph 1 is literal, 2 and 3 are slots, so an editor can rewrite the founding story while an invisible paragraph above contradicts them. **Structural point underneath all three: going editable didn't create the duplication, it removed the only tool that was catching it** — three greppable strings became three database rows, `--check` compares repo to repo and `slotcheck` reads the CDN, so neither can see two sections disagree. **Recommendation is delete, not sync:** a fact with one home can't drift, so let `community-stats` be the only place a count appears and rewrite the other two to carry the claim without the arithmetic. Where a fact genuinely must repeat, cross-reference it **bidirectionally** in the `doc` strings — they surface in wp-admin, so the warning lands where the editing happens. **Nobody erred here:** Finn's `doc` on `bnb_body_2` already flags that field's own count; what's missing is a cross-section view, and no file in the project has a place to hold one.
- DONE: 2026-08-13 · **The `.ln` clipping NEED from 2026-08-10 is approved and with Finn** — Conti gated it as correctness rather than design, having read `brgw.css:162` rather than taking my description, and asked for a fixture (an accented capital proving it no longer clips) rather than a before/after showing nothing moved. Worth recording *why* a four-month-old measurement suddenly mattered: **hero headlines became editable today, and that converted a risk I had measured into one nobody can.** The mask holds 1.034em, Blanco's `é` is 1.132em; that was fine while every headline was in git and I'd checked them all, and stopped being fine the moment Sean could type `CAFÉ` into the field. Conti also took my correction that a wrapped decorated span is **clipped, not split** — every headline runs `data-head="words"`, so each line already sits inside an `overflow:hidden` `.ln`, and `box-decoration-break` rescues `.hl` only because a background is a box property and a drawn stroke isn't — and has passed it to Finn as a system rule: **text-attached decoration binds only to a span guaranteed not to wrap; everything else binds to the section box.** The decoration spec itself is **not commissioned** — Sean hasn't ruled and I'm not starting it.
- PLAN: 2026-08-13 · **SPEC-007 — [the content gaps holding up launch](explorer/content-gaps.md).** Three gaps, one cause: each is **a fact the site asserts that nobody maintains**, and each survives because *nothing objects*. Proposing a **doctrine** first, since fixing these without it just grows three more: **(1) a placeholder must fail loudly in the build and invisibly on the page** — `XX` does the exact inverse, invisible to every check we own and glaring to every visitor, which is why the password gate is currently doing a build check's job; **(2) no fragment ships a fact it can't keep true** — relative dates, follower counts, durations are absolute-or-deleted, since a static CDN fragment has no render-time computation. **The single most useful finding: `community-stats` should stop blocking launch.** I modelled the grid's border rules at 2/3/4 cells — `1fr 1fr` ≥700px, `border-right` except `:nth-child(2n)`, `border-bottom` except `:nth-last-child(-n+2)` — and **3 cells breaks structurally**: cell 2 loses its bottom rule and cell 3 sits alone in the left column with a vertical rule pointing at empty space. 2 and 4 are both clean. So **Sean needs one more number or three, never two** — and one of them (**team headcount across SD & OC**) exists in HR today with zero research, so `12` + headcount is a clean 2-cell section available now. **Rejecting "meals/dollars donated" outright rather than filling it** — a dollar figure for private philanthropy is the exact number that gets quoted back at you, and the slash between two incompatible units is the tell that neither was available.
- PLAN: 2026-08-13 · **SPEC-006 — [Contact: the section plan](explorer/contact-page.md).** Never designed, only listed — so the deliverable is the decision that gates it, not a comp reading. **Recommend a routing page, not a form page.** The form isn't the free option it looks like: our fragment is authored on Netlify and rendered on the WP origin, so **Netlify Forms would submit cross-origin and navigate the visitor off `blacktoprestaurantgroup.com` onto a Netlify success page** *(origin split is certain; exact submit behaviour I reasoned rather than measured — worth a live test before anyone commits)*. The honest form path is a WP form plugin placed beside our shortcode, which lands outside `.brgw-shell` and so inherits the theme's styles, not `brgw.css`. Separately a form is probably the wrong instrument: arrivals are sorting into press / partnerships / careers / guest-recovery, and **two of those four routes already exist and are live** (`hello@…?subject=Community%20partnership`, `/careers/`). **Three sections, zero new archetypes** — hero, a `home-values` card grid where the label becomes the link, and a `cta-band` pointing at Our Restaurants (which catches the likeliest arrival of all: someone wanting a restaurant's hours). **It also defuses a live time-bomb:** `sections.json:27` defaults `community-partner`'s `cta_href` to `/contact/` — inert today because the fragment has no `{{tokens}}`, but Conti measured on 2026-08-13 that the moment `acf-slot-tokens` lands, deployed v2.5.0 renders that CTA as `Get in touch` → **a 404**. Ship Contact or change the default; either way it's Conti's file. **Contact is the only unbuilt thing on this project not blocked on content BRG has to go produce** — decent argument for building it *before* Press.
- PLAN: 2026-08-13 · **SPEC-005 — [Press & Gallery: the section plan](explorer/press-gallery-page.md).** **Press is cheap** — four sections, and the entire net-new surface is *the article card interior, the gallery tile, and one colour token*. `press-hero` is `hero-page` (~11 lines, CSS already shared); `press-inquiries` is **`careers-positions` mirrored** — same band, same inverted black `.btn`, teal instead of yellow, cutout on the left; `press-gallery` is `team-members`' grid geometry minus the overlay; `press-articles` reuses the `careers-posts` shell (same watermark, same centred card column) with a new card inside. **The comp reads as five bands but builds as four** — the smiley badge straddling the hero seam is a `.seam` owned by the *lower* section bleeding **up**, exactly as `home-community` does, not a section of its own. **Architectural flag for Finn:** the bolt and BRG cluster hang **downward** out of the teal band, which is the direction the bleed rule doesn't do — a later sibling paints over them. Recommend they live in `press-gallery` and bleed up (rule intact, no z-index exception) rather than granting `press-inquiries` the `team-apply` exception. **Two content traps:** the comp's card contains one word — `ARTICLE`, centred in an empty half — which is a placeholder, not a layout (real card wants thumbnail · outlet · headline · date, whole card as the `<a>`, plus a visible external-link cue); and **the page must not be blocked on BRG having press** — hero → inquiries → gallery is a complete page, so `press-articles` should be built and held out of `stacks.press` until there are ≥2 real articles. One article in a two-card layout looks like a mistake; zero looks like a decision.
- QUESTION: 2026-08-13 · @Conti — **Press needs a fifth marker colour we don't have, and my 2026-08-10 colour-drift question now has a third source agreeing against us.** I sampled the comp rather than eyeballing it: hero underline **`#5D0E8B`** (violet — no token exists), Media Inquiries band **`#00BEB5`**, top rule and `.hl` highlight **`#FAE200`**, card ink `#231F20` (✓ matches `--ink`). So the comp lands **with Sean's `assets/lines/` SVGs** (`#00BEB4`, `#FAE200`) and **against `brgw.css`** (`--teal:#19C7C2`, `--yellow:#FCE200`) — two independent design-source files agreeing with each other is no longer drift I can call ambiguous, and I'd move the tokens to the artwork values. It touches every page, so it's your direction call. **Also: `assets/media/lines/` holds exactly ten files, two per existing page — there is no `line-press-*` or `line-contact-*`**, so if SPEC-004's pen-stroke underline is adopted, both new pages have no artwork to draw and need four more exports from Sean. Neither blocks: both pages use the current CSS `.brgw-uline`, as all five live pages still do. SPEC-006 §2.1 argues Contact should **reuse** pink rather than invent a seventh colour, precisely so the marker set stays a system rather than a palette.
- NEED: 2026-08-13 · @Sean — **the four content asks, ranked by what actually unblocks something.** (1) **Team headcount across SD & OC** — HR has it today, no research, and it's the one figure that takes `community-stats` from three `XX`s to a clean 2-cell section immediately (SPEC-007 §2.3). (2) **BRG's LinkedIn company slug** — one lookup; `careers-posts`' two `View job` buttons currently point at `/careers/`, i.e. **the page they're already on**, so that's a live dead control on the section whose whole job is to convert. Company jobs URL is correct today, correct after any posting expires, and needs no maintenance. (3) **Permission to delete `"834 followers · 2mo"`** — the fragment's own comment confirms `.meta` deletes with no layout consequence, and an ageing stamp on a job post signals *the role is stale*, which is the opposite of the section's purpose. (4) **Photography** — full brief in SPEC-007 §3, including the constraints the CSS already imposes (all three `home-different` plates are greyscale; **only `home-community`'s large plate is in colour**, so it carries all the warmth; `object-fit:cover` centre-crops, so deliver loose; each plate parallaxes independently, so edge subjects drift out of frame). **And one thing worth doing regardless of a shoot: `home-different`'s stand-ins actively contradict its copy** — the section argues BRG is a different kind of *employer* and illustrates it with a stock skater and surfer, which reads as lifestyle brand. A phone photo of the real crew, greyscaled, beats good stock here, because the section's whole claim is authenticity.
- DONE: 2026-08-10 · **Fixed the study — Sean saw the strokes appear, not animate, and he was right.** Two causes, both mine, both worth carrying into production: (1) I triggered per **section**, but the artwork sits ~350px below the section top, so the draw completed off-screen and you only ever met the finished stroke; (2) the page hard-depended on GSAP, and if it doesn't load nothing arms the paths, so every stroke renders complete at first paint — same symptom, different cause. Rewrote it to **observe the artwork itself with IntersectionObserver and animate `stroke-dashoffset` in CSS — zero library**, which is the §3 implementation I'm recommending anyway, so the study now demonstrates the recommendation instead of the alternative. Lesson for the engine: **trigger on the element, not its section**, whenever the element isn't near the section's top edge.
- PLAN: 2026-08-10 · **SPEC-004 §7 — split-text pairing + the Blanco crop, measured.** Sean's read is right on both counts. Study §8 now runs the **real production markup** (`.ln` `overflow:hidden` `padding:.30em .10em .16em`, `.ln-i` from `translateY(160%)`) in the **real Blanco**, marker at +280ms so it lands after the last line rather than racing it. Added a **live clip audit** that measures ink against the actual mask box in the actual DOM — nothing hardcoded. Result: headroom **+0.163em** top / **+0.202em** bottom; current copy's tallest ink (`’`) 0.872em; **mask holds 1.034em; the face's tallest glyph (`é`) is 1.132em.** So **today's copy doesn't clip, but the face doesn't fit the mask** — any accented capital gets shaved by ~0.098em with no warning. Horizontal is fine (0.067em overhang vs 0.10em padding), descenders fine (0.184 vs 0.346).
- NEED: 2026-08-10 · @Finn — **one line in `brgw.css`: `.ln{padding:.42em .10em .16em;margin:-.42em -.10em -.16em}`** (was `.30em` top). Negative margin already cancels it so spacing is untouched; `.42em` clears the whole face with room. **Raising the *top* padding is safe for the reveal** — the line travels up from `+160%` and stops at rest, so it's never above rest position and can't leak early; that reasoning does *not* apply to `padding-bottom`, so leave that at `.16em`. **QUESTION alongside it:** I could not reproduce a clip in today's copy, so if Sean saw one on a specific headline my padding finding may not be the cause. Leading suspect is the **font gate**: `splitByWords` groups lines by measuring `offsetTop`, and `startRevealGate` falls through on a 1800ms race / 3500ms hard timeout — if Blanco resolves *after* that, grouping is measured in Montserrat metrics, a line ends up wider than `.ln`, and `overflow:hidden` cuts its end off horizontally. That would look exactly like a cropped font. Worth testing on a throttled connection before we call it closed.
- PLAN: 2026-08-10 · **SPEC-004 — [pen-stroke underline](explorer/pen-stroke-underline.md)** + working study [`studies/pen-stroke-underline.html`](explorer/studies/pen-stroke-underline.html) (7 sections, every variant verified rendering — mid-draw and final). Built per Conti's `PLAN:`. **Recommend variant 1 (single marker) as the default** for every hero + nav active item at 0.72s `power2.out` (nav 0.34s); two-pass reserved for one deliberate moment per page; rough marker on hold pending Sean; scribble-circle blocked on real artwork (§4 — **there is no scribble-circle anywhere in `website/mocks/`**; the loop in the study is mine, drawn in the idiom, and shouldn't ship as-is). **The mock SVGs are filled outlines too**, so I derived the centerlines: each is a uniform stroke expanded to outline, so *centerline = leading edge offset by half the cap height*. Yields 6 real markers — and two build-changing facts: it's **three shapes, not six** (home/our-restaurants/community are the same curve, so one path + `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"` stretches to any width), and **careers is authored right→left** so it writes backwards unless drawn from the far end.
- DECISION: 2026-08-10 · **Correcting myself from SPEC-003 §5.** I argued against GSAP partly because the plugin inlines `brgw.js` and inline script isn't cacheable per-page. Finn didn't bundle it — GSAP is self-hosted in `assets/vendor/` behind `<script src>`, which *is* cached across pages. That objection doesn't hold; the cost is one cached ~116KB download, not a per-page tax. Also superseding SPEC-003 §1: now that real stroked centerlines exist, **the draw beats the clip-wipe** — at 42% the marker has a live rounded leading edge the wipe can't produce (it shows a flat vertical edge). SPEC-003 stands for the nav `a[href]` wiring and the file-location/colour-token flags.
- QUESTION: 2026-08-10 · @Conti — **the site-wide GSAP call (SPEC-004 §6).** `startMotion()` loads GSAP only when a page matches `MOTION_SEL` (`[data-brgw-img]/[data-brgw-parallax]/[data-brgw-pin]`) — but **every hero has an underline**, so routing it through GSAP quietly turns a lazy dependency into a site-wide one. For a single open path with `pathLength="1"`, `drawSVG:0→100%` and `stroke-dashoffset:1→0` are **identical output** — the study runs dashoffset and looks exactly like your plan intends. **Recommend (A):** do it in CSS on `.reveal.is-in` (~6 lines, no JS at all, since the IntersectionObserver already sets that class), and keep GSAP lazy for scrub work — holding DrawSVG in reserve for the scribble-circle, which is multi-loop and may want partial draws. If you'd rather have (B), the study already auto-detects `window.DrawSVGPlugin` and switches. **NEED either way:** `DrawSVGPlugin.min.js` is not in `assets/vendor/` (only gsap + ScrollTrigger) — B can't be built until it's added, and I've not downloaded it; that's your call or Sean's.
- NEED: 2026-08-10 · @Finn — **two capture traps from SPEC-004 §7, same family as your `.anim-head` font-gate one.** (1) **ScrollTrigger's `onEnter` never fires for anything already above the start line at first paint** — i.e. the hero, on every page. IntersectionObserver reports already-visible elements on observe; ScrollTrigger only fires on *crossing*. Cost me a "broken" study that wasn't. Needs an explicit initial sweep — the study has one, commented. (2) **GSAP's ticker is rAF-driven and rAF is frozen in headless/background capture** — `gsap.ticker.frame` sat at **4** after two seconds, every tween stuck at progress 0, page looked broken. Drive motion deterministically for screenshots (set progress explicitly) instead of sleeping. Both bite GSAP motion only; the CSS approach in §3 is immune, which is a quiet argument for (A).
- PLAN: 2026-08-10 · **SPEC-003 — [hand-drawn underlines + the DrawSVG question](explorer/hand-drawn-lines.md).** Sean dropped 10 SVGs in `assets/lines/` (nav + lg per page) and asked whether GSAP DrawSVG can draw them. **It can't, as exported** — all ten are a single *filled outline* path, `fill="none"` on the svg, **zero stroke attributes in any of them**; DrawSVG animates `stroke-dasharray`/`dashoffset`, so there's nothing to dash. Forcing a stroke traces the blob's outline out-and-back (reads as a lasso, not a pen). Recommend **`clip-path` wipe on the filled path — no re-export, no dependency**: these are all monotonic left→right, so wipe and true-draw are visually the same here, and it **fixes a live bug** — animating the current `scaleX(0→1)` ([brgw.css:78–82](../../website/assets/brgw.css)) on these would squash the round caps into ellipses for the whole transition. Confirmed the mapping is unambiguous: `-lg` fills land 1:1 on the per-page hero underline colours Finn shipped, and the `-nav` set is per-word-width teal at ~5px, matching the **6.3px active underline Finn measured off the comp**. Centerline IS recoverable (uniform ~5px stroke + round caps, expanded) if we ever want true draw — re-export unexpanded.
- NEED: 2026-08-10 · @Finn — two things in SPEC-003 §6 before any of this builds. (1) **The line files are at repo-root `assets/lines/`, outside the Netlify publish dir — they are not on the CDN and would 404.** They belong at `website/assets/media/lines/` where `netlify.toml` already sends ACAO:* + a 1-year cache. (2) **Inline the 5 `-lg` files, link the 5 `-nav` ones** — heroes want `fill="currentColor"` so your existing `color:var(--yellow|teal|pink|orange)` per-hero rule keeps driving them, and `currentColor` doesn't survive `background-image`/`<img>`; they're 413–849 bytes, cheaper inlined than requested. Bonus: **the nav underline needs no plugin change** — the plugin emits bare `<a href>` with no slug class, but `a[href="/team/"]::after` discriminates fine, so the whole nav swap is `brgw.css` only and isn't behind the deploy gate. Check what `pages.json` actually emits for Home first (the `/brg-home/` url override), don't assume `/`.
- QUESTION: 2026-08-10 · @Conti — **our colour tokens don't match Sean's artwork, and all four drift the same way**: teal `#00BEB4` vs `--teal:#19C7C2`, yellow `#FAE200` vs `#FCE200`, pink `#F40085` vs `#EC0F8D`, orange `#F87513` vs `#F5821F`. The SVGs came out of the design source so I'd treat them as authoritative and the tokens as drift — but that's direction, it touches every page, and `brgw.css` tokens are the whole palette. Not blocking: SPEC-003 uses `currentColor`, so the lines follow whatever you rule.
- DECISION: 2026-08-10 · **GSAP: no for this effect** — and I read that as consistent with your standing rule (Conti `PLAN:` 2026-08-10 "reach for GSAP when a section warrants it"; Finn's "no by default, yes for a specific named effect"), not a new call. Three reasons in SPEC-003 §5: DrawSVG can't act on these files at all; even re-exported, `stroke-dashoffset` is a few lines of vanilla JS; and the cost is worse here than the usual 70KB argument because **`vcc_shared_assets()` inlines `brgw.js` into the page body — inline script isn't cacheable across pages**, so GSAP would re-download on every page view forever to replace one CSS property. Where I'd still spend it: scroll-*scrubbed* draw, pinned sections, MorphSVG-class work — and loaded as an external cacheable `<script src>`, never inlined into the shared bundle.
- DONE: 2026-08-10 · **SPEC-001 amended per Conti's `DECISION:` — assignment complete.** (a) §7 phases inverted: Phase 1 is now the **5 Careers sections only** (`hero-page`, `badge-rule`, `perks-list`, `apply-button`, `cta-band`), Phase 2 the screenshot-diff go/no-go, Phase 4 the remaining harvest — plus a column marking which phases the deploy gate actually blocks (only the mu-plugins upload; Phases 1–2 are local and unblocked). (b) **B4 struck** from §3 and its `$seen`/strip-`<style>` code removed from plugin change #3 — Conti is right that scoped `.brgw-sec--<id>` CSS makes a duplicate block idempotent and that the strip-regex was the same markup-regex failure mode my own §8 rejects; my spec contradicted itself and the amendment fixes it. (c) Motion-parity reasoning recorded in §7 (stagger is per-`.reveal`, brgw.js:40–45; §4 rule 2 = one `.reveal` per section root, so cadence reproduces by construction) + Finn's `.anim-head{opacity:0}` capture trap. (d) §9 marked resolved with the Q1–Q4 answers; §5 gained the new contract rule — **slot defaults are real production copy, never placeholders**, since the harness can't see WP-side values.
- QUESTION: 2026-08-10 · @Conti — three things I've parked in **[SPEC-001 §10](explorer/stacking-sections.md)** rather than dropped; none block Phases 1–2, all want an answer before Phase 4 puts stacked pages live. (1) A stacked page has no single "page" identity — `[brg_header active=…]` covers nav, but `<title>`/meta/OG come from WP, so `pages.json` stops describing what a page *contains*; probably a `STATUS.md` line when Phase 4 lands (your file). (2) `hero-page` carries the `<h1>` — stack two heroes and you get two `<h1>`s; I'd add an `as="h1|h2"` slot when a second hero actually appears, not pre-emptively. (3) SPEC-002 #11 `stat-strip` stays harvestable-but-unshippable while three of four numbers read literal `XX`.
- DONE: 2026-08-10 · Noted the short names — **Expo** (me), **Conti**, **Finn**. Recorded in `notes/explorer/README.md`; already in project memory and in MANIFESTO's Roles table (`07e6fab`), so no clearance needed from me.
- NEED: 2026-08-09 · @Finesser — apologies: my `git add notes/` swept your in-flight `notes/finesser/compose.mjs` into my commit `d4e5f31`. Nothing was changed or lost (131 lines, `node --check` passes) — it's just already tracked on `main` before you committed it. Your `.gitignore` edit is still unstaged and untouched. **Gotcha for all three of us, same family as the `git add -A` one in STATUS:** `git add notes/` sweeps the other chats' uncommitted files. Stage your own file explicitly — `git add notes/<yours>.md` — not the folder. I'll do that from here.
- QUESTION: 2026-08-09 · @Controller — four calls in [SPEC-001 §9](explorer/stacking-sections.md): (Q1) slots yes/no — I say yes but only heading/sub/CTA/anchor, body+grids stay in git; (Q2) `[brg_footer]` explicit vs auto-inject via a `the_content` filter — I say explicit; (Q3) cold-cache budget — cap ~8 sections/page + section TTL 600s?; (Q4) `brgw-sec` / `brgw-sec--*` need adding to MANIFESTO §Shared tokens (your file).
- NEED: 2026-08-09 · @Controller — SPEC-001 needs 7 plugin changes in `vc-clients-embed.php` (yours), bumping it to v2.1.0. One is a refactor of existing code: lift the `static $shared` asset-dedupe out of `vcc_render_page` (:101–110) into `vcc_shared_assets()` so a section renderer shares the same static — otherwise a page mixing a page + sections inlines `brgw.css`/`brgw.js` twice. Worth landing that refactor first, on its own. Reminder: the plugin is a manual upload to `/wp-content/mu-plugins/` — pushing the repo doesn't update it.
- PLAN: 2026-08-09 · **SPEC-002 — [section inventory](explorer/section-inventory.md).** Read all 5 fragments; catalogued **14 section archetypes**, 6 already duplicated across pages (`hero-page`, `badge-rule`, `cta-band`, `intro-lede` + 2). Ranked harvest order; flagged 4 that should NOT become slot-driven (people-grid, media-slider, value-grid, perks-list — grids/lists stay in git). `hero-page` is the cheapest first harvest: its CSS is already shared in `brgw.css`.
- PLAN: 2026-08-09 · **SPEC-001 — [stacking sections](explorer/stacking-sections.md)** (the priority topic) — full spec: `website/sections/<id>/embed.html` + `website/sections.json` manifest, `[brg_section id="…"]` / `[brg_s_<id>]` / `[brg_header active="…"]` / `[brg_footer]`, `{{slot}}` substitution whitelisted + escaped by manifest-declared type, and a 4-phase non-breaking rollout (go/no-go = a stacked Careers page matching `[brg_careers]` on screenshot-diff). Verified in code what already works (per-request asset dedupe :101; multi-root reveal init brgw.js:55–62; recursion guard; stale-while-error) and what blocks it (**B1** chrome duplicates on every stacked shortcode :113–115, **B2** slug sanitiser strips slashes so there's no section path :90/:97, **B3** dedupe static trapped in `vcc_render_page`, **B4** same section twice = duplicate `<style>`, **B5** nav `is-active` has no slug on a stacked page, **B6** N sections = N 8s-timeout fetches cold). Key design call: **all section CSS scoped under `.brgw-sec--<id>`, so sections add zero new global class names** and the MANIFESTO's uniqueness rule needs no per-section reservation.
- DONE: 2026-08-09 · Read `MANIFESTO.md`, `STATUS.md`, `notes/controller.md`, `notes/finesser.md`, plus the plugin, `brgw.css`, `brgw.js` and all 5 page fragments. Stood up **[`notes/explorer/`](explorer/)** as the Explorer workspace (README + numbered specs); this file remains the log the Controller scans.
- PLAN: 2026-08-09 · (closed by SPEC-001 below) Spec the "stacking sections" model — section-level fragments a WP page composes from several shortcodes. Infra already dedupes shared CSS/JS across stacked BRG shortcodes; define a section manifest + shortcode form for the Controller to wire into the plugin.
