# Notes — EXPLORER (research / ideas / experiments)

**Single writer: the Explorer chat only.** Others READ this; they write their own `notes/*.md`.
Research direction/sitemap/content/best-practices and propose sections & features as **SPECS —
don't ship production code.** Log proposals as `PLAN:` for the Controller to approve (`DECISION:` in
`notes/controller.md`); the Finesser builds approved ones.
Newest first. Format: `TYPE: <YYYY-MM-DD> · note`  (TYPE = PLAN / DONE / NEED / DECISION / QUESTION).
Long-form specs live in **[`notes/explorer/`](explorer/)** — this file stays a scannable log.

- PLAN: 2026-08-10 · **SPEC-004 — [pen-stroke underline](explorer/pen-stroke-underline.md)** + working study [`studies/pen-stroke-underline.html`](explorer/studies/pen-stroke-underline.html) (7 sections, every variant verified rendering — mid-draw and final). Built per Conti's `PLAN:`. **Recommend variant 1 (single marker) as the default** for every hero + nav active item at 0.72s `power2.out` (nav 0.34s); two-pass reserved for one deliberate moment per page; rough marker on hold pending Sean; scribble-circle blocked on real artwork (§4 — **there is no scribble-circle anywhere in `website/mocks/`**; the loop in the study is mine, drawn in the idiom, and shouldn't ship as-is). **The mock SVGs are filled outlines too**, so I derived the centerlines: each is a uniform stroke expanded to outline, so *centerline = leading edge offset by half the cap height*. Yields 6 real markers — and two build-changing facts: it's **three shapes, not six** (home/our-restaurants/community are the same curve, so one path + `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"` stretches to any width), and **careers is authored right→left** so it writes backwards unless drawn from the far end.
- DECISION: 2026-08-10 · **Correcting myself from SPEC-003 §5.** I argued against GSAP partly because the plugin inlines `brgw.js` and inline script isn't cacheable per-page. Finn didn't bundle it — GSAP is self-hosted in `assets/vendor/` behind `<script src>`, which *is* cached across pages. That objection doesn't hold; the cost is one cached ~116KB download, not a per-page tax. Also superseding SPEC-003 §1: now that real stroked centerlines exist, **the draw beats the clip-wipe** — at 42% the marker has a live rounded leading edge the wipe can't produce (it shows a flat vertical edge). SPEC-003 stands for the nav `a[href]` wiring and the file-location/colour-token flags.
- QUESTION: 2026-08-10 · @Conti — **the site-wide GSAP call (SPEC-004 §6).** `startMotion()` loads GSAP only when a page matches `MOTION_SEL` (`[data-brgw-img]/[data-brgw-parallax]/[data-brgw-pin]`) — but **every hero has an underline**, so routing it through GSAP quietly turns a lazy dependency into a site-wide one. For a single open path with `pathLength="1"`, `drawSVG:0→100%` and `stroke-dashoffset:1→0` are **identical output** — the study runs dashoffset and looks exactly like your plan intends. **Recommend (A):** do it in CSS on `.reveal.is-in` (~6 lines, no JS at all, since the IntersectionObserver already sets that class), and keep GSAP lazy for scrub work — holding DrawSVG in reserve for the scribble-circle, which is multi-loop and may want partial draws. If you'd rather have (B), the study already auto-detects `window.DrawSVGPlugin` and switches. **NEED either way:** `DrawSVGPlugin.min.js` is not in `assets/vendor/` (only gsap + ScrollTrigger) — B can't be built until it's added, and I've not downloaded it; that's your call or Sean's.
- NEED: 2026-08-10 · @Finn — **two capture traps from SPEC-004 §7, same family as your `.anim-head` font-gate one.** (1) **ScrollTrigger's `onEnter` never fires for anything already above the start line at first paint** — i.e. the hero, on every page. IntersectionObserver reports already-visible elements on observe; ScrollTrigger only fires on *crossing*. Cost me a "broken" study that wasn't. Needs an explicit initial sweep — the study has one, commented. (2) **GSAP's ticker is rAF-driven and rAF is frozen in headless/background capture** — `gsap.ticker.frame` sat at **4** after two seconds, every tween stuck at progress 0, page looked broken. Drive motion deterministically for screenshots (set progress explicitly) instead of sleeping. Both bite GSAP motion only; the CSS approach in §3 is immune, which is a quiet argument for (A).
- PLAN: 2026-08-10 · **SPEC-003 — [hand-drawn underlines + the DrawSVG question](explorer/hand-drawn-lines.md).** Sean dropped 10 SVGs in `assets/lines/` (nav + lg per page) and asked whether GSAP DrawSVG can draw them. **It can't, as exported** — all ten are a single *filled outline* path, `fill="none"` on the svg, **zero stroke attributes in any of them**; DrawSVG animates `stroke-dasharray`/`dashoffset`, so there's nothing to dash. Forcing a stroke traces the blob's outline out-and-back (reads as a lasso, not a pen). Recommend **`clip-path` wipe on the filled path — no re-export, no dependency**: these are all monotonic left→right, so wipe and true-draw are visually the same here, and it **fixes a live bug** — animating the current `scaleX(0→1)` ([brgw.css:78–82](../../website/assets/brgw.css)) on these would squash the round caps into ellipses for the whole transition. Confirmed the mapping is unambiguous: `-lg` fills land 1:1 on the per-page hero underline colours Finn shipped, and the `-nav` set is per-word-width teal at ~5px, matching the **6.3px active underline Finn measured off the comp**. Centerline IS recoverable (uniform ~5px stroke + round caps, expanded) if we ever want true draw — re-export unexpanded.
- NEED: 2026-08-10 · @Finn — two things in SPEC-003 §6 before any of this builds. (1) **The line files are at repo-root `assets/lines/`, outside the Netlify publish dir — they are not on the CDN and would 404.** They belong at `website/assets/media/lines/` where `netlify.toml` already sends ACAO:* + a 1-year cache. (2) **Inline the 5 `-lg` files, link the 5 `-nav` ones** — heroes want `fill="currentColor"` so your existing `color:var(--yellow|teal|pink|orange)` per-hero rule keeps driving them, and `currentColor` doesn't survive `background-image`/`<img>`; they're 413–849 bytes, cheaper inlined than requested. Bonus: **the nav underline needs no plugin change** — the plugin emits bare `<a href>` with no slug class, but `a[href="/team/"]::after` discriminates fine, so the whole nav swap is `brgw.css` only and isn't behind the deploy gate. Check what `pages.json` actually emits for Home first (the `/brg-home/` url override), don't assume `/`.
- QUESTION: 2026-08-10 · @Conti — **our colour tokens don't match Sean's artwork, and all four drift the same way**: teal `#00BEB4` vs `--teal:#19C7C2`, yellow `#FAE200` vs `#FCE200`, pink `#F40085` vs `#EC0F8D`, orange `#F87513` vs `#F5821F`. The SVGs came out of the design source so I'd treat them as authoritative and the tokens as drift — but that's direction, it touches every page, and `brgw.css` tokens are the whole palette. Not blocking: SPEC-003 uses `currentColor`, so the lines follow whatever you rule.
- DECISION: 2026-08-10 · **GSAP: no for this effect** — and I read that as consistent with your standing rule (Conti `PLAN:` 2026-08-10 "reach for GSAP when a section warrants it"; Finn's "no by default, yes for a specific named effect"), not a new call. Three reasons in SPEC-003 §5: DrawSVG can't act on these files at all; even re-exported, `stroke-dashoffset` is a few lines of vanilla JS; and the cost is worse here than the usual 70KB argument because **`vcc_shared_assets()` inlines `brgw.js` into the page body — inline script isn't cacheable across pages**, so GSAP would re-download on every page view forever to replace one CSS property. Where I'd still spend it: scroll-*scrubbed* draw, pinned sections, MorphSVG-class work — and loaded as an external cacheable `<script src>`, never inlined into the shared bundle.
- DONE: 2026-08-10 · **SPEC-001 amended per Conti's `DECISION:` — assignment complete.** (a) §7 phases inverted: Phase 1 is now the **5 Careers sections only** (`hero-page`, `badge-rule`, `perks-list`, `apply-button`, `cta-band`), Phase 2 the screenshot-diff go/no-go, Phase 4 the remaining harvest — plus a column marking which phases the deploy gate actually blocks (only the mu-plugins upload; Phases 1–2 are local and unblocked). (b) **B4 struck** from §3 and its `$seen`/strip-`<style>` code removed from plugin change #3 — Conti is right that scoped `.brgw-sec--<id>` CSS makes a duplicate block idempotent and that the strip-regex was the same markup-regex failure mode my own §8 rejects; my spec contradicted itself and the amendment fixes it. (c) Motion-parity reasoning recorded in §7 (stagger is per-`.reveal`, brgw.js:40–45; §4 rule 2 = one `.reveal` per section root, so cadence reproduces by construction) + Finn's `.anim-head{opacity:0}` capture trap. (d) §9 marked resolved with the Q1–Q4 answers; §5 gained the new contract rule — **slot defaults are real production copy, never placeholders**, since the harness can't see WP-side values.
- QUESTION: 2026-08-10 · @Conti — three things I've parked in **[SPEC-001 §10](explorer/stacking-sections.md)** rather than dropped; none block Phases 1–2, all want an answer before Phase 4 puts stacked pages live. (1) A stacked page has no single "page" identity — `[brg_header active=…]` covers nav, but `<title>`/meta/OG come from WP, so `pages.json` stops describing what a page *contains*; probably a `STATUS.md` line when Phase 4 lands (your file). (2) `hero-page` carries the `<h1>` — stack two heroes and you get two `<h1>`s; I'd add an `as="h1|h2"` slot when a second hero actually appears, not pre-emptively. (3) SPEC-002 #11 `stat-strip` stays harvestable-but-unshippable while three of four numbers read literal `XX`.
- DONE: 2026-08-10 · Noted the short names — **Expo** (me), **Conti**, **Finn**. Recorded in `notes/explorer/README.md`; already in project memory and in MANIFESTO's Roles table (`07e6fab`), so no clearance needed from me.
- NEED: 2026-08-09 · @Finesser — apologies: my `git add notes/` swept your in-flight `notes/finesser/compose.mjs` into my commit `d4e5f31`. Nothing was changed or lost (131 lines, `node --check` passes) — it's just already tracked on `main` before you committed it. Your `.gitignore` edit is still unstaged and untouched. **Gotcha for all three of us, same family as the `git add -A` one in STATUS:** `git add notes/` sweeps the other chats' uncommitted files. Stage your own file explicitly — `git add notes/<yours>.md` — not the folder. I'll do that from here.
- QUESTION: 2026-08-09 · @Controller — four calls in [SPEC-001 §9](explorer/stacking-sections.md): (Q1) slots yes/no — I say yes but only heading/sub/CTA/anchor, body+grids stay in git; (Q2) `[brg_footer]` explicit vs auto-inject via a `the_content` filter — I say explicit; (Q3) cold-cache budget — cap ~8 sections/page + section TTL 600s?; (Q4) `brgw-sec` / `brgw-sec--*` need adding to MANIFESTO §Shared tokens (your file).
- NEED: 2026-08-09 · @Controller — SPEC-001 needs 7 plugin changes in `vc-clients-embed.php` (yours), bumping it to v2.1.0. One is a refactor of existing code: lift the `static $shared` asset-dedupe out of `vcc_render_page` (:101–110) into `vcc_shared_assets()` so a section renderer shares the same static — otherwise a page mixing a page + sections inlines `brgw.css`/`brgw.js` twice. Worth landing that refactor first, on its own. Reminder: the plugin is a manual upload to `/wp-content/mu-plugins/` — pushing the repo doesn't update it.
- PLAN: 2026-08-09 · **SPEC-002 — [section inventory](explorer/section-inventory.md).** Read all 5 fragments; catalogued **14 section archetypes**, 6 already duplicated across pages (`hero-page`, `badge-rule`, `cta-band`, `intro-lede` + 2). Ranked harvest order; flagged 4 that should NOT become slot-driven (people-grid, media-slider, value-grid, perks-list — grids/lists stay in git). `hero-page` is the cheapest first harvest: its CSS is already shared in `brgw.css`.
- PLAN: 2026-08-09 · **SPEC-001 — [stacking sections](explorer/stacking-sections.md)** (the priority topic) — full spec: `website/sections/<id>/embed.html` + `website/sections.json` manifest, `[brg_section id="…"]` / `[brg_s_<id>]` / `[brg_header active="…"]` / `[brg_footer]`, `{{slot}}` substitution whitelisted + escaped by manifest-declared type, and a 4-phase non-breaking rollout (go/no-go = a stacked Careers page matching `[brg_careers]` on screenshot-diff). Verified in code what already works (per-request asset dedupe :101; multi-root reveal init brgw.js:55–62; recursion guard; stale-while-error) and what blocks it (**B1** chrome duplicates on every stacked shortcode :113–115, **B2** slug sanitiser strips slashes so there's no section path :90/:97, **B3** dedupe static trapped in `vcc_render_page`, **B4** same section twice = duplicate `<style>`, **B5** nav `is-active` has no slug on a stacked page, **B6** N sections = N 8s-timeout fetches cold). Key design call: **all section CSS scoped under `.brgw-sec--<id>`, so sections add zero new global class names** and the MANIFESTO's uniqueness rule needs no per-section reservation.
- DONE: 2026-08-09 · Read `MANIFESTO.md`, `STATUS.md`, `notes/controller.md`, `notes/finesser.md`, plus the plugin, `brgw.css`, `brgw.js` and all 5 page fragments. Stood up **[`notes/explorer/`](explorer/)** as the Explorer workspace (README + numbered specs); this file remains the log the Controller scans.
- PLAN: 2026-08-09 · (closed by SPEC-001 below) Spec the "stacking sections" model — section-level fragments a WP page composes from several shortcodes. Infra already dedupes shared CSS/JS across stacked BRG shortcodes; define a section manifest + shortcode form for the Controller to wire into the plugin.
