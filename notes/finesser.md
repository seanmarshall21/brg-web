# Notes — FINESSER (hands-on build: page fragments + shared engine)

**Single writer: the Finesser chat only.** Others READ this; they write their own `notes/*.md`.
(If we ever run two Finessers, split into `notes/finesser-pages.md` + `notes/finesser-lib.md`.)
Newest first. Format: `TYPE: <YYYY-MM-DD> · note`  (TYPE = DONE / PLAN / NEED / DECISION / QUESTION).

Before you build: `git pull`, read `MANIFESTO.md` + `STATUS.md` + `notes/controller.md` +
`notes/explorer.md`. Consume the shared reveal engine / slider / doodles — don't rebuild them.
Clear shared-asset (`brgw.css`/`brgw.js`), plugin, or `pages.json` changes through the Controller
(log a `NEED:`) before they land. Compose-test + screenshot before `DONE:`.

- DONE: 2026-08-10 · **First 5 stacked sections built + verified: all five heroes.** `website/sections/{home,our-restaurants,team,community,careers}-hero/embed.html`, per the SPEC-001 §4 contract (one root, no `.brgw` wrapper, CSS scoped under `.brgw-sec--<id>`, root carries `reveal`, no section JS). Sean's ruling: **page-scoped, hyphenated ids** — `[brg_home-hero]` — so the plugin's existing `[a-z0-9-]` sanitiser needs no change; my earlier underscore `NEED:` is closed. Per "similar hero builds, but different heroes", the shared look stays in `.brgw-hero` in `brgw.css` and each section only carries what differs (photo plate, underline colour, which doodles).
- DONE: 2026-08-10 · **Real brand icons replace the hand-drawn ones.** The old heroes inlined my *approximations* of the bolt/asterisk/smiley as hand-written SVG paths. Now using the actual artwork from Sean's drop (`icn-misc-*.svg`) via `<img>`. Extended `.brgw-doodle`/`.brgw-badge` rules in `brgw.css` to cover `img` as well as `svg`, reduced-motion rule included.
- DONE: 2026-08-10 · **Harness composes stacked pages** (`compose.mjs --stack`), assembling header-shell + N section-shells + footer-shell exactly as SPEC-001 §6 specifies, from `notes/finesser/sections.draft.json`. Verified all 5 pages stacked, desktop + mobile, no warnings: 3 top-level `.brgw` roots each initialised exactly once, per-`.reveal` stagger intact, doodles correctly hidden < 560px. **This is the Phase-3 architecture proof for the hero tier** — the multi-root reveal behaviour Expo predicted in SPEC-001 §2 holds in practice.
- DONE: 2026-08-10 · Drafted `notes/finesser/sections.draft.json` — all 19 section ids + the per-page stack order + status. **@Conti this is the hand-up for `website/sections.json`; 5 are `live`, 14 `todo`.**
- DONE: 2026-08-10 · Harness reliability fix: `shot.mjs` now reaps orphaned Chrome processes from crashed runs before launching. Those orphans held their debug port and competed for CPU — the cause of both the "Chrome did not expose its debug port" failures and the phantom "reveal engine never ran" warnings on the first shot of a run (the page was fine; the readiness poll was starved). Also added `--probe`, which dumps per-root init state and per-heading opacity, which is what actually diagnosed it.
- QUESTION: 2026-08-10 · **Mobile nav wraps to two rows and eats ~170px of the fold** (visible in `team--stacked-mobile.png`) — five links at that letter-spacing don't fit 390px. Wants a hamburger or a scrollable single row. The markup is the plugin's (`vcc_chrome`, Conti) but the CSS is mine, so I can do a CSS-only fix; flagging rather than assuming since it changes the look of every page.
- DONE: 2026-08-10 · Sean's rulings applied: `Swell` `@font-face` **removed** (not needed now), and `--yellow` **stays `#FCE200`** — the logo artwork gets corrected later instead, so my `#FAE200` QUESTION is closed with no token change.
- DONE: 2026-08-10 · **Real assets are now served.** `assets/` was 36MB, untracked, and sat OUTSIDE `website/` — the Netlify publish dir — so nothing in it was reachable by a page. Copied (not moved; the original folder is untouched on disk) to `website/assets/media/`, converted 16 heavy rasters to webp and dropped 4 redundant jpg/webp pairs: **36MB → 10MB**. Biggest offender was a 6.4MB hero JPEG.
- DONE: 2026-08-10 · **Fonts self-hosted** in `brgw.css` — dropped the `fonts.googleapis.com` `@import` AND the `blacktoprestaurantgroup.com/wp-content/uploads/…` `@font-face`. Two external deps gone; a WP media reshuffle can no longer break the display face. `netlify.toml` already sent `Access-Control-Allow-Origin:*` for `/*.woff2`, which is exactly what makes the cross-origin load work. Also registered `Swell` (new file, not yet used anywhere — see QUESTION below).
- DONE: 2026-08-10 · **Real brand lockup in header + footer**, swapped via CSS background so it needs no plugin change; the old text markup stays as the accessible name, indented out of view. Verified: nav logo + Blanco + Montserrat all render from the CDN paths. Screenshot `notes/finesser/.out/shots/home-desktop-fold.png`.
- DONE: 2026-08-10 · Harness: local server now serves `/assets/**` from the repo and rewrites the CDN-absolute asset URLs to local, so a compose-test is faithful *before* a push instead of showing the live (or missing) files. `--live` still hits the real CDN.
- NEED: 2026-08-10 · **@Conti — ratify three shared-code changes** (human-directed by Sean this turn, so they're built, but they're your call to keep): (1) self-hosted fonts in `brgw.css`; (2) the CSS logo swap in `.brgw-logo` / `.brgw__footer .lockup`; (3) `font-weight:800` → `700` in those two rules, since I only self-host 400/500/600/700 and 800 was only ever used by the placeholder text lockups — leaving it would render faux-bold.
- NEED: 2026-08-10 · **@Conti — the mockup's section ids break the plugin's sanitiser.** Figma labels them `[brg_home_hero]`, `[brg_careers_apply]` … with **underscores**, but `vcc_render_page`/alias registration strips to `[a-z0-9-]` (:90), so `home_hero` → `homehero`. One-char fix in your file (add `_` to the charset) or we use `home-hero` and the shortcode becomes `[brg_home-hero]`. Your call — I've built nothing that depends on it yet.
- NEED: 2026-08-10 · **@Conti — the mockup diverges from SPEC-001 §4 and it's worth a ruling before I harvest.** SPEC-001 approved *reusable archetypes + slots* (`hero-page`, `cta-band` used across pages). Sean's Figma annotates **page-scoped sections** (`home_hero`, `team_hero`, `careers_hero` as separate sections). Page-scoped is simpler and matches the comps, which genuinely differ per page; but it drops the reuse that motivated slots, and 19 sections is a lot of near-duplicate CSS. My read: **go page-scoped for the ids Sean specified** (it's his build), and let the shared *look* live in `brgw.css` (`.brgw-hero` already does this) rather than in a slot system. Wants your 👍 since it rewrites §4/§5.
- QUESTION: 2026-08-10 · Two brand facts I won't decide unilaterally. (1) **The real logo's yellow is `#FAE200`; our `--yellow` token is `#FCE200`.** BUILD-SPEC always said "confirm exact hex from the .ai swatches" — this is that confirmation arriving, and it means every yellow on the site is currently 2 points off the logo. Changing `--yellow` is a shared-token change, so it's yours. (2) **`Swell-Regular.woff2` is a new face** in Sean's asset drop that no spec mentions — registered but unused pending a steer on where it belongs.
- DONE: 2026-08-10 · Decoded the full **stacked-section manifest** from the Figma annotation columns (the rotated margin labels): home `hero→community→about→different→values`; our-restaurants `hero→brands`; team `hero→members→apply`; community `hero→give→stats→partner`; careers `hero→apply→positions(→posts on the extended state)`; plus `[brg_nav]` + `[brg_footer]` on all six. 19 sections. Heights recorded at 1920w. This is the build order.
- DONE: 2026-08-09 · Onboarded. Read `MANIFESTO.md`, `STATUS.md`, `notes/controller.md`, `notes/explorer.md` + `notes/explorer/README.md`. Stood up my workspace `notes/finesser/` (see its README) — outside `website/` on purpose, since that's the Netlify publish dir.
- DONE: 2026-08-09 · Built the compose-test harness the "verify before DONE" rule needs, so it's one command per turn instead of ad-hoc: `notes/finesser/compose.mjs` rebuilds a page exactly as the plugin renders it (mirrors `vcc_render_page()` + `vcc_chrome()` — nav from `pages.json`, `.is-active`, shared assets inlined in order; `--live` composes from the CDN to check what actually deployed), and `notes/finesser/shot.mjs` captures desktop 1440 + mobile 390 full-page. Zero deps — drives system Chrome over CDP via Node's built-in WebSocket. Output `.out/` gitignored (one line added to root `.gitignore`).
- DONE: 2026-08-09 · Baseline verified: all 5 pages compose and render correctly, desktop + mobile — header/nav/active-link, Blanco headlines, hero photo reveal, community slider + dots, footer. Shots in `notes/finesser/.out/shots/`.
- NEED: 2026-08-09 · Controller — nothing to approve yet; flagging only that the harness added one line to root `.gitignore` (`notes/finesser/.out/`). No `website/`, `brgw.css`, `brgw.js`, `pages.json`, or plugin changes this turn.
- QUESTION: 2026-08-09 · Two traps worth knowing before anyone screenshots a BRG page (both cost me a bad shot): (1) **only `brgw.js` clears `.anim-head{opacity:0}`** — CSS never does — so a capture before the font gate resolves silently drops every display headline while still looking like a valid page; `shot.mjs` now polls for it and warns rather than shipping a bad shot. (2) Headless Chrome throttles background-tab timers, which starves that same font gate. Noting here so the Controller/Explorer don't re-derive it.
- NEED: 2026-08-09 · Controller — **the Explorer and I are working in the same checkout (`~/Documents/GitHub/brg-web`, both on `main`) at the same time, and it showed.** Its `d4e5f31` swept up my still-in-progress `notes/finesser/compose.mjs` and committed it under an Explorer message; harmless this time (file landed intact, verified `git diff HEAD` clean), but it's a race that will eventually commit someone's half-written file or clobber an edit. Single-writer *note files* don't protect the working tree. Suggest one of: each chat commits with an explicit pathspec for its own area only (never a bare `git add -A`/`git add notes`), or each chat gets its own worktree — there are already two sitting unused under `~/Documents/Claude/_Code/brg-web/`. Your call; I'll follow whichever you set.
- QUESTION: 2026-08-09 · Ready for direction. Absent a steer, my default next turn is the two visible content gaps already tracked in `STATUS.md` item 3: the community stats still read literal `XX`, and the give-back slider's right half is an empty image placeholder. Both are content-blocked (real numbers + photos), not code — say the word if you'd rather I take an Explorer spec or a polish pass instead.
