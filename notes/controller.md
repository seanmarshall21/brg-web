# Notes — CONTROLLER (source of truth / oversight)

**Single writer: the Controller chat only.** Others READ this; they write their own `notes/*.md`.
Newest first. Format: `TYPE: <YYYY-MM-DD> · note`  (TYPE = DONE / PLAN / NEED / DECISION / QUESTION).

- DECISION: 2026-08-10 · **SPEC-001 (stacking sections) APPROVED to build, with four amendments.**
  (a) **Invert Phases 2↔3** — harvest ONLY the sections Careers needs first {`hero-page`, `badge-rule`, `perks-list`, `apply-button`, `cta-band`}, prove parity, then harvest the remaining archetypes. Front-loads the contract test the Finesser flagged. (b) **Drop B4** (strip `<style>` on repeat) for now — scoped `.brgw-sec--<id>` CSS makes a duplicate block idempotent; the strip-regex is the same markup-regex failure §8 rejects. If a page ever uses a section twice AND the bytes matter, hoist section CSS to a collected head-inject properly. So plugin change #3 ships WITHOUT the `$seen`/strip-style bit. (c) **Motion parity** confirmed in code (per-`.reveal` stagger, one `.reveal` per section) — Finesser runs the screenshot-diff at the Phase-3 go/no-go with the harness they built. (d) Amendments (a)+(b) are edits to SPEC-001 §7/§3 — **@Explorer please amend your spec** to record them.
- DECISION: 2026-08-10 · **Sequencing gate — nothing modifies the deployed plugin until v2.0.0 is verified live.** Agree with the Finesser: shipping v2.1.0 (or even the vcc_shared_assets refactor) on top of an unverified v2.0.0 baseline means we can't isolate a regression. I may WRITE v2.1.0 in the repo (versioned), but **no upload to `/wp-content/mu-plugins/` until one `[brg_<slug>]` page renders live showing `<!-- vc_embed brg/<slug> v2.0.0 -->` + nav.** Same human step already blocking go-live; buys a known-good baseline for free.
- DECISION: 2026-08-10 · **Working tree — pathspec staging (option a), not worktrees.** Ownership is directory-disjoint, so explicit per-area pathspecs fix the demonstrated root cause (`git add notes/` sweeping in-flight files) while keeping the shared-`main` "pull every turn sees everyone" model intact. Codified in MANIFESTO §Working protocol. Worktrees stay as an opt-in escape hatch for big sweeps (Phase 4).
- DECISION: 2026-08-10 · **SPEC-001 §9 answers.** Q1 slots: **yes but few** — `heading`/`sub`/`cta_label`+`cta_href`/`anchor` only; body/lists/grids stay in git. **Adopting the Finesser's refinement as a contract rule:** since the compose harness can't see WP-stored slot values, **`sections.json` defaults must be the real production copy, not placeholders**, so a default compose-test represents the live page. Q2 `[brg_footer]` **explicit** — approved. Q3 cold-cache — **cap ~8 sections/page, section TTL 600s** (ttl=0/`?brg_refresh=1` still bust during builds). Q4 `brgw-sec`/`brgw-sec--*` — **added to MANIFESTO §Shared tokens** this turn.
- DONE: 2026-08-10 · MANIFESTO edits: Q4 section tokens added; ownership table extended (`website/sections.json`→Controller, `website/sections/<id>/embed.html`→Finesser); staging-discipline rule with per-role pathspecs.
- PLAN: 2026-08-10 · **Assignments.** @Finesser — best unblocked turn is to **front-load the Phase-3 contract proof locally** (not the content gaps, which are still content-blocked): harvest the 5 Careers sections into `website/sections/<id>/embed.html` per the §4 contract (scoped CSS, root `reveal`, no `.brgw` wrapper), extend your harness to assemble a stacked Careers from those section files, and screenshot-diff it vs monolith `[brg_careers]` desktop+mobile. Draft the `sections.json` (with **real** default copy) in your workspace and hand it up as a `NEED:` — I own/ratify the real `website/sections.json`. If local parity holds, I write plugin v2.1.0 immediately (deploy still gated on v2.0.0 live-verify). @Explorer — amend SPEC-001 §7/§3 per the phase-inversion + B4-deferral above.
- DONE: 2026-08-09 · Added `website/wp-snippets/brg-password-gate.php` — BRG-branded WP password gate (Oxygen-safe auto-gate + `[brg_password]` shortcode), reskinned from the Temper gate. Native WP postpass, independent of the embed plugin. Install = WPCode PHP snippet "Run Everywhere" (or a mu-plugin file). Lock via Pages → Quick Edit → Password.
- DECISION: 2026-08-09 · Adopted the Explorer/Finesser/Controller model (from the TEMPER handoff), adapted for BRG. This chat is the Controller. Finesser owns page fragments + shared `brgw.css`/`brgw.js`; Explorer proposes sections/features as specs. Shared-asset / plugin / `pages.json` changes clear through me before landing. See `MANIFESTO.md`.
- DONE: 2026-08-09 · Stood up `MANIFESTO.md`, `STATUS.md`, `notes/{manager,finesser,explorer}.md` at repo root.
- DONE: 2026-08-09 · Community give-back converted to auto-advancing slider + reusable `.brgw-slider` engine; doodles hidden < 560px (mobile motion audit). Pushed `f1b3c8e`.
- NEED: 2026-08-09 · Human: create the 5 WP pages and drop `[brg_<slug>]` on each (all `/<slug>/` are 404 today). Blocks go-live + live plugin verification.
- PLAN: 2026-08-09 · Once one WP page exists, verify `<!-- vc_embed brg/<slug> v2.0.0 -->` + nav render live, then greenlight the other four.
