# Notes — CONTROLLER (source of truth / oversight)

**Single writer: the Controller chat only.** Others READ this; they write their own `notes/*.md`.
Newest first. Format: `TYPE: <YYYY-MM-DD> · note`  (TYPE = DONE / PLAN / NEED / DECISION / QUESTION).

- DONE: 2026-08-10 · **v2.1.0 verified LIVE** — `v2.1.0` markers on community + team, zero literal shortcodes. Section layer is registered and rendering. Only 7/18 section fragments exist (5 heroes + careers-apply + careers-positions), so the other 11 render invisible `<!-- not built yet -->` until harvested.
- DONE: 2026-08-11 · **Automated the WP drops (Sean: "so I don't have to go drop it in").** (1) **ACF — fully automated.** New install-once loader `website/wp-mu-plugin/brg-acf.php` registers the options page + FETCHES `all.acf.json` from Netlify and `acf_add_local_field_group()`s each — so add/change a field = edit sections.json → `kit/build-acf.py` → push → live, **no import ever**. Safe (fetches DATA, not code; netlify-only, cached). `build-acf.py` now also emits `website/acf/all.acf.json`. Superseded + deleted `brg-section-content-options.php` (options page folded into brg-acf.php). (2) **Plugin CODE — can't fetch-and-run PHP (RCE); use deploy-on-push.** Added `.github/workflows/deploy-mu-plugins.yml` — SCP's `website/wp-mu-plugin/*.php` → the server's mu-plugins dir on push (needs 4 one-time secrets: WP_SSH_HOST/USER/KEY + WP_MU_PLUGINS_PATH; DreamHost SSH). Trade-off flagged: auto-deploy removes the manual verify gate — can restrict to `workflow_dispatch`. Reality: plugin code churn is now low (core built; most change is data/fragments/ACF which already auto-update).
- DONE: 2026-08-11 · **ACF-editable sections (Sean: "each section comes with PHP + an acf.json to edit content").** Built the mechanism, matching Temper's options-page pattern — and BRG needs **no per-section PHP** (the plugin fills any section generically). Pieces: (1) `website/wp-snippets/brg-section-content-options.php` — registers ONE "Section Content" ACF options page (menu_slug `brg-section-content`); (2) plugin **v2.5.0** — `vcc_fill_slots` now ACF-aware: precedence **shortcode attr > `get_field('brg_<id>_<slot>','option')` > default**, image fields resolve to URL; (3) `kit/build-acf.py` — generates a per-section `website/acf/brg-<id>.acf.json` from the section's `slots` in `sections.json` (location `options_page==brg-section-content`, matched so it can't silently show nowhere); (4) demo: `community-partner` slots declared (heading/sub/cta_label/cta_href) → `brg-community-partner.acf.json` generated + validated. **Workflow** in kit/README. **HUMAN:** upload plugin v2.5.0 + install the options-page snippet (needs ACF Pro) + import each acf.json. @Finn: as you build/edit a section, put `{{slots}}` in the fragment for the editable bits + declare them in sections.json; I regenerate the acf.json. Field-name contract = `brg_<id-with-underscores>_<slot>` (plugin + generator derive it identically).
- DONE: 2026-08-11 · **Adopted the fc-brands registry→generate process for BRG ("your call").** New `kit/registry.json` = single source of truth for all 7 shortcodes; `kit/build.py` generates `SHORTCODES.md` + `docs/shortcode-index.html` from it and stamps each component with `version` + `contract` (hash of shortcode+type+attr names/defaults) — same pattern as Temper. `--check` fails on a moved-but-unbumped contract or stale docs (verified: editing an attr default trips it); `--restamp` bumps + re-hashes. `kit/README.md` documents the loop. **Docs are now generated — edit the registry, run `python3 kit/build.py` in the same commit as any shortcode change; don't hand-edit SHORTCODES.md / shortcode-index.html.** Contract ledger (v1): page `88180b5dc4cf`, page_generic `50710ec86127`, section `22483f6c6833`, section_generic `9d639f65bd4c`, nav `e4a3ae5fc13b`, footer `2246471ccc36`, vc_embed `9bc191093a53`. Next candidate: close the nav gaps (`register` attr + WP submenu/dropdown support) — deferred.
- DONE: 2026-08-11 · **Ingested the fc-brands upstream reference (Sean pointed us at the Temper system repo).** Read-only clone; read `temper/kit/registry.json` (source of truth), the nav contract + coupling table, and the registry→validate→generate process. Recorded a **reference ledger** in `notes/upstream-fc-brands.md` with contract hashes for drift detection (nav v1 `11d3f98b3055`, password v1 `8c6b407d5c6c`, footer, fc_embed). Key findings: (1) **BRG's fresh nav avoided all 4 of their silent-failure traps** (baked texture, hardcoded ticketing URL → wrong event, menu-location name collision, fonts-not-loaded → Arial) — validates building fresh vs copying. (2) **Nav attr gaps vs theirs:** `register` (auto-invert; we have the mechanism, no attr) + `dropdowns` (WP submenu support — we ignore child items) are worth closing; `radius` minor; `tickets/tixlabel/meta/mtix` are Temper-specific. (3) **Process to steal:** they generate docs from `registry.json` so they never drift — BRG hand-keeps `SHORTCODES.md`/`shortcode-index.html` (same drift risk). @Sean: two offers — (a) close the nav gaps (register attr + submenu/dropdown support); (b) adopt a BRG registry→generate pipeline. Questions to their team route through Sean (we can't post in their notes).
- DONE: 2026-08-11 · **Mobile menu polish (Sean: "fix the menu icon + slide-out, a lot of things just pause").** Hamburger now MORPHS to a clean X on open (was a flow-layout chevron; switched to absolute-positioned bars sharing the center line, rotate ±45) + toggles open/close. Drawer: smoother ease + `will-change`; items **stagger rise-in** (per-item transition-delay set in JS + reflow before open) — kills the dead pause after the slide. Ham keeps its register color (dark on white bar, white on transparent/frost); mobile close = the morphing X (drawer Close hidden < 860). CSS/JS only → **no plugin re-upload; push deploys it** (or `?brg_refresh=1`). Diagnostic gotcha logged: headless `--force-device-scale-factor=2` makes innerWidth wider than `--window-size`, cropping the right edge — verify mobile nav at scale 1. NOTE: Sean's other app is building a MASTER component folder (source of truth) I'll pull from later to align/replace these — kept this a focused fix.
- DONE: 2026-08-11 · **Nav v2.4.0 — fixed the sticky bug + background attributes (Sean).** ROOT CAUSE of "sticky isn't working": each shortcode is wrapped in its own short `.brgw-shell`, so a `position:sticky` nav un-sticks with that tiny parent. Fix: `.bnav` is now `position:fixed` (like Temper) + `brgw-nav.js` reserves an equal spacer on the wrapper when the bg is opaque so content isn't hidden. New attrs: `bg="solid|none|frost"` (solid=white bar+reserve; none=transparent float; frost=dark frosted glass+blur+float), `bgcolor="#hex"`, `opacity="0–1"` (via `--bnav-bg`/`--bnav-op` on a `::before` layer; transparent/frost float + white text). Compose-tested all 3 over a dark hero (screenshots). Docs → v2.4.0. **HUMAN: upload plugin v2.4.0.** Example: `[brg_nav layout="center" bg="frost" sticky="hide"]`.
- DONE: 2026-08-11 · **Nav v2.3.0 — layout control attributes (Sean, matching Temper).** `[brg_nav]` now takes `layout="left|split|center|compact"`, `left`/`right` (per-side counts, overflow → **More** drawer), `sticky="pin|hide"` — same attribute names as Temper. Plugin reads attrs → emits `class="bnav lay-…" data-left data-right data-sticky` + `.bnav-grp-a/-b` slots + hidden `.nav-src` source. `brgw-nav.js` rewritten to distribute items per layout (ported from Temper's render()), build the More drawer (doubles as mobile takeover), keep the pen-stroke underline + active state. Compose-tested left/center/compact (screenshots: logo centers in center, More overflow works, underline draws). Fixed two bugs found in test: grid cells followed DOM order (→ explicit `grid-column`), and underlines escaped (→ `.bnav-item{position:relative}`). Docs updated (`SHORTCODES.md`, `docs/shortcode-index.html`, artifact). **HUMAN:** upload plugin **v2.3.0** → `/wp-content/mu-plugins/`. Default layout = `left` (matches the export). Still not built: CTA pill (`cta`/`ctalabel`), footer options.
- DONE: 2026-08-10 · **Built the WP-menu-driven nav (plugin v2.2.0 + `brgw-nav.css`/`brgw-nav.js`).** `[brg_nav]` now runs `wp_nav_menu(theme_location=brg_primary, menu_class=nav-src)`; plugin registers locations **BRG — Primary** + **BRG — Social** (Appearance → Manage Locations). New `.bnav` component (Conti-owned, namespaced away from Finn's `.brgw-*` in brgw.css) — white bar, yellow top rule, logo-left, items-right; `brgw-nav.js` injects the **pen-stroke marker underline** per item + marks active (WP `current-menu-item` + URL-match fallback) + builds the mobile takeover. Underline **draws on hover AND active** via CSS stroke-dashoffset (no GSAP dep for the nav). Compose-tested against a real `wp_nav_menu`-shape menu (screenshot: active "Our Story" + hovered "Team" both draw). Nav assets added to the client `assets` array (inlined once/page). Structurally validated (bracket lexer 50/50/330/330/87/87; `node --check` on the JS). **HUMAN steps:** (1) upload plugin v2.2.0 → `/wp-content/mu-plugins/` (replace v2.1.0); (2) Appearance → Menus → create "BRG — Primary", add/order items + links, assign to the location. Then the nav renders from WP. Mobile takeover is basic v1 (staggered-reveal polish = follow-up). `[brg_footer]` unchanged.
- DECISION: 2026-08-10 · **BRG nav is WordPress-MENU-driven, not `pages.json` (Sean, matches Temper).** `[brg_nav]` must run `wp_nav_menu()` for a registered location (Appearance → Menus + Manage Locations); the user manages items/order/links/hierarchy in WP — nothing hard-coded. This SUPERSEDES the pages.json-driven `vcc_chrome` nav and moots the "Our Story = Home?" IA question (those are WP menu items now). My first-cut prototype already emits the Temper `<ul class="nav-src">` contract, which is exactly what `wp_nav_menu(menu_class=nav-src)` outputs — so the component drops in; only the DATA SOURCE changes. Build: (1) @Conti plugin → `register_nav_menus(['brg_primary','brg_social'])` + rewrite `[brg_nav]` to `wp_nav_menu(theme_location=brg_primary, container=false, menu_class=nav-src, fallback_cb=false)` wrapped in nav chrome (bump v2.2.0). (2) @Conti/@Finn reskinned `brgw-nav.css`/`brgw-nav.js` (styling + enhancer: marker underline active, More-overflow, auto-invert, mobile takeover) — "styling pushes live from repo, content lives in WP." (3) Human: create "BRG — Primary" menu + assign to the location. `pages.json` stays ONLY for page/section shortcode registration.
- PLAN: 2026-08-10 · **Reuse Temper's nav system for BRG (Sean).** Read the fc-brands mirror (`_fc-brands-mirror/notes/ranger.md`): Ranger built a **generic, reskinnable nav** — `reference/temper-nav/desktop-nav.html` + `mobile-menu.html` (copied into our repo). Contract: `wp_nav_menu`-driven `<ul class="nav-src">`, tokenized `--acc`/`--cond`, config via data-attrs (`data-left`/`data-right` per-side counts → overflow **More** drawer; `data-mtix="bar|menu"`; mobile `data-tix="bar|top|inline|bottom|end"`). Features: transparent nav that **auto-inverts** over dark/light sections (`.on-dark/.on-light`, per-section `data-nav`), layout modes (left/split/center/compact), CTA pill (Temper=Tickets), morphing hamburger, split-text reveal. **Auto-invert is a perfect fit — BRG already stacks dark/light sections.** BRG reskin: `--acc`→yellow+teal, `--cond`→Montserrat/Blanco, logo→our `logos/logo-brg-nav-sm.svg`, CTA→(TBD: Order/Contact/Careers), feed = pages.json/sections.json. Routing: @Expo spec the BRG nav (reskin + expansions: which CTA, default layout, doodle accents, on-light mapping to our sections); @Conti upgrade `[brg_nav]`/`vcc_chrome` to emit the `nav-src` contract + enqueue the reskinned CSS/JS; @Finn build the reskinned `brgw-nav` CSS/JS from the template. Freeze the Temper class/data-attr contract so WP wiring doesn't rework.
- PLAN: 2026-08-10 · **Pen-stroke underline (Sean).** Upgrade the current `.brgw-uline` (a `scaleX` blob) to a real marker **stroke-draw**. Technique: inline SVG `<path>` of the actual Figma marker stroke, animate `stroke-dashoffset` (set `pathLength="1"`). **Production = GSAP DrawSVGPlugin** (now free): `gsap.from(path,{drawSVG:0})` triggered on scroll-in via ScrollTrigger, folded into the reveal engine (`.reveal.is-in`). Variants studied (widget demo): single marker (default), rough marker (feTurbulence displacement for ink wobble), two-pass emphasis (staggered), scribble-circle a word (special callouts). Reduced-motion → show final state. Paths come from the exported section SVGs (real marker shapes). @Finn owns the engine change; @Expo can prototype variants in a sandbox. NOTE: adds GSAP as a dependency to the reveal engine — decide load strategy (CDN vs bundled, once site-wide) before shipping; keep the CSS-only fallback for no-JS/reduced-motion.
- DONE: 2026-08-10 · **ALL 18 section exports landed in `website/mocks/` (Sean)** — every `sections.json` id has a matching `brg_<id>.svg` + `@2x.png`, plus `nav`/`footer` and all 5 full-page PNGs. Full build source; Finn can now build every section on every page. Extras (do NOT add to the manifest — internal refs only): `community-give-intro`/`-slider1`/`-slider2` are the internals of the ONE `community-give` slider fragment (build with the shared `brgw-slider`); `careers-positions-extd` is a variant ref of `careers-positions`. mocks now 107MB, still gitignored.
- DONE: 2026-08-10 · **ALL graphics exported + live on the CDN (Sean).** Correction to my earlier note: the **crown is NOT new** — it's at `assets/media/bg/crown-white.webp` (+ `crown.webp`), HTTP 200 on the CDN. No extraction/standalone export needed. Full set now in `website/assets/media/` and deployed: `icons/` (asterisk/bolt/smiley/BRG-cluster, each in div-/clust-/misc- variants, SVG+PNG), `bg/` (crown, skater/surfer/car photos, grit texture, and **`bg/base/base-{390..1920}.webp`** = the responsive grayscale hero collage), `bkgnds/` (graffiti patterns), `logos/`. Per Sean: **everything graphic is exported; anything NOT exported is a CSS/styling decision, not an asset.** So @Finn has zero asset gaps — Home hero bg = `bg/base/base-<w>.webp` (responsive), crown = `bg/crown-white.webp`, doodles = `icons/*`, watermark = `bkgnds/brg-pattern-*`. All prior "export X" NEEDs are closed.
- DONE: 2026-08-10 · **Figma section exports landed in `website/mocks/`** (Sean) — full-page 2× PNGs (all 5) + Home sections as SVG **and** @2x PNG (hero, community, about, different, values, nav, footer), named to our section ids. Quality is excellent; SVGs are true vector (text outlined, doodles/watermark as extractable paths). **gitignored** (88MB, would deploy to CDN from the publish dir) — local ref only, shared via the checkout. @Finn: these are the build source for Home; match layout + doodle placement to them. Findings: (1) Home hero HAS a **spray-paint crown** doodle top-right — NOT in our icon set, it's new (I was wrong earlier). (2) Body sections have a **full graffiti watermark background** + seam badges — the watermark = our existing `brg-pattern-*` assets (`assets/media/bkgnds/brg-pattern-blk-long`, `brg-pattern-gry-trns.svg`); apply those as section bg. NEED from Sean: export any doodle/graphic NOT already in `assets/media/icons` (the crown, and any others) as its own SVG; we already have asterisk/bolt/smiley/BRG-cluster + the patterns.
- DECISION: 2026-08-10 · **Cross-section bleed rule (stacking model).** Sections are independent stacked fragments; no element spans two. A design element that crosses a seam **lives in the LOWER (later) section and bleeds UPWARD** (`overflow:visible` + absolute, negative `top`) — later sections paint on top, so upward bleed shows; downward bleed gets covered by the lower section's bg and is disallowed. Ex: Careers surfer → owned by `careers-positions`, bleeds up over `careers-apply`. @Finn build to this.
  **EXCEPTION — shared chrome (footer/nav):** when the neighbor is the shared `[brg_footer]` (or `[brg_nav]`), ownership flips to the PAGE-SPECIFIC section, which bleeds DOWN (or up into nav) — the doodle can't live in the shared, page-agnostic chrome. To make the downward bleed paint over the footer's bg, give the owning section `position:relative; z-index:2; overflow:visible` and absolutely-position the doodle with negative `bottom`. Instance: Team `team-apply` BRG-cluster (`icons/icn-clust-all-trim.svg`) bleeds down into the footer → owned by `team-apply`. **Verify this bleed on the LIVE page, not just the harness — the Oxygen `ct-shortcode` wrapper (outside the harness) could clip it.** Export: whole doodle stays in the page-specific section's export (transparent), footer export stays clean. Sean is exporting per-section (transparent-bg) refs named to our section ids into `website/mocks/`; bleeding elements go in ONE export (the owner), not split. Doodles are placement-only refs — we already have the SVG icons; only export a doodle as SVG if it's not in `assets/media/icons`.
- DONE: 2026-08-10 · **Figma source for doodle placement** (Sean shared the link). File `J5WEM71T5ssMjq6CCaQmLQ` ("Blacktop Restaurant Group"). Finding: it's a deeply-nested Illustrator paste — doodles + sections are un-named groups (`Vector`/`Group`/`clippath`), so **clean per-doodle coordinate extraction isn't feasible; match visually per frame.** Authoritative page frame node-ids (pull each with the Figma connector / `get_screenshot`, maxDimension ~2000): **Home 66:7325 · Our Restaurants 66:9499 · Team 66:10578 · Community 66:11925 · Careers 66:13668 · Careers Extd 66:14628.** @Finn — for each section you harvest, pull its page frame and place the doodles (icon + spot + size) to match; the flattened comp `mocks/brg-pages-r1-sections.jpg` is the same layout if you prefer it. Note: most doodles live in the BODY sections (per the comp), and Home hero has none in Figma — the current per-hero corner doodles are placeholder.
- NEED: 2026-08-10 · @Finn — two from Sean: (1) **Harvest the 11 `todo` sections** — that's why pages show hero-then-blank; the shortcodes are registered but the fragments don't exist. Prioritize highest-visibility first (home body sections, community-give/stats). (2) **Doodle placement must follow the Figma comp `website/mocks/brg-pages-r1-sections.jpg`, not the corner formula.** Current heroes use a generic `top:~20% right:9%` / `bottom:~17% left:8%` on every hero — that's placeholder. Per the comp, doodles are specific per section (icon + spot + size) and MOST live in the body sections (smiley+BRG in "Born in San Diego", asterisks in "different kind", BRG watermark in Core Values, bolt by give-back band…), so place them as you harvest each body section, and correct the hero doodles to the comp. Animations themselves are fine to keep. Sean may share the Figma link → Conti can pull exact node coords.
- DECISION: 2026-08-10 · **@Finn — your v2.1.0 spec and mine converged; answers to all four points.** (1) v2.1.0 already ships exactly your register-set: `[brg_<id>]` alias form (not `[brg_s_<id>]`), `[brg_nav]`/`[brg_footer]`, generic `[brg_section id=]`. (2) **Ratified your draft → `website/sections.json`** (18 ids incl. `our-restaurants-brands`, which I'd missed — that page was still a monolith when I checked). Push deploys it; the plugin registers all 18 so nothing renders literal. (3) **`[brg_nav]` active-state + B5 home slug: solved** — chrome auto-derives active from the current WP slug AND matches the manifest `url`; I added `"url":"/brg-home/"` to home in `pages.json`, so home highlights on `/brg-home/` and the nav/logo link there. Explicit `[brg_nav active="…"]` also supported. (4) **Chrome still emits the TEXT logo markup** (`<b>BLACKTOP</b><span>…</span>`), NOT an `<img>` — verified. **Keep your `.brgw-logo`/`.brgw__footer .lockup` CSS swap; it won't double up.**
- DECISION: 2026-08-10 · **Sequencing gate SATISFIED — greenlight the v2.1.0 upload.** @Finn your gate concern is already answered: I verified v2.0.0 live on our-restaurants earlier (`vc_embed brg/our-restaurants v2.0.0` + nav + hero), which also proves **the plugin IS installed** (an uninstalled plugin couldn't have produced that marker). The base is verified; no scratch page needed. Upload v2.1.0 whenever the Netlify manifest deploy is done.
- NEED: 2026-08-10 · @Expo — please amend SPEC-001 §6.5/§6.6 to reality (as-built in v2.1.0): alias form is `[brg_<id>]` (not `[brg_s_<id>]`); chrome is `[brg_nav]` (no-attr, auto-active + optional `active=`) and `[brg_footer]`, not `[brg_header active=]`. Finn's underscore NEED is closed.
- PLAN: 2026-08-10 · Tooling: Sean uploaded **GSAP + three.js skills**. Current reveal engine is bespoke CSS/IntersectionObserver — fine as-is. @Finn (motion owner): reach for the GSAP skills for richer section/hero motion when a section warrants it (scroll-scrub, timelines), three.js only if a hero ever needs it; keep the shared reveal engine as the baseline so non-JS/reduced-motion still degrades cleanly.
- DONE: 2026-08-10 · **Plugin v2.1.0 written** (`vc-clients-embed.php`). Verified v2.0.0 live first (our-restaurants: marker+nav+hero — deploy gate met). Discovery: the 4 other WP pages are already composed from **section shortcodes** (`[brg_nav]`, `[brg_<page>-<section>]`×N, `[brg_footer]`), rendering literal because v2.0.0 lacks them. v2.1.0 adds: `[brg_nav]`/`[brg_footer]` chrome (auto-highlight via current slug / `active=`), `vcc_render_section()` + `[brg_<id>]` aliases + generic `[brg_section id=]`, `vcc_shared_assets()` refactor (dedupe across stacked shortcodes), `vcc_fill_slots()` ({{slot}} whitelist by type), nav `Home→/brg-home/` via a `url` override in pages.json, ttl/refresh honored on sections. Created `website/sections.json` (17 ids: 5 live heroes + 12 stubs — all registered so no page shows a literal token). No PHP locally to `php -l`; validated by a proper PHP-aware bracket lexer (45/45/300/300/76/76, all strings/comments closed) + mirrors proven v2.0 patterns.
- NEED: 2026-08-10 · @Sean (human) — **upload the new `website/wp-mu-plugin/vc-clients-embed.php` to `/wp-content/mu-plugins/`** (replace v2.0.0). Push deploys `sections.json`/`pages.json` to Netlify first, so upload after the Netlify deploy finishes. Marker will read `v2.1.0` when it's live.
- NEED: 2026-08-10 · @Finn — harvest the **12 stub sections** listed in `sections.json` (home-about/different/values/community, team-members/apply, community-stats/give/partner, careers-positions/posts/apply) into `website/sections/<id>/embed.html` from the existing monolith page fragments, per SPEC-001 §4 (scoped `.brgw-sec--<id>`, root `reveal`, no `.brgw` wrapper). Heroes already exist. Until then those slots render invisible.
- DONE: 2026-08-10 · Prep while go-live is human-blocked: (1) `netlify.toml` — `X-Robots-Tag: noindex` on the CDN host so raw fragments don't become duplicate content vs the WP pages; hard cache for `/assets/media/*` + woff2; bare-root 302 → blacktoprestaurantgroup.com. (2) `scripts/verify-live.sh` — one command to confirm all pages render live (HTTP 200 + `vc_embed` marker + nav), reads slugs from `pages.json`. (3) `GO-LIVE.md` — click-by-click WP page-creation runbook (the one blocking step). Added `netlify.toml` + `scripts/` + `GO-LIVE.md` to Controller's owned pathspec.
- NEED: 2026-08-10 · @Finn — asset divergence to reconcile (your area, flagging not editing): the section fragments reference **SVG** icons/logos (e.g. `icn-misc-ast-circle-wht.svg`), so the PNG/WebP icons+logos are likely **unused** on the live site. Also `website/assets/media/` has WebP for bkgnds(5)/imgs(9) but icons/logos are still PNG, and repo-root `/assets` (where I ran the conversion) diverges from the deploy path. Suggest: decide the canonical media set (SVG for icons/logos, WebP for photos), delete the unused raster icons/logos, and make sure everything referenced actually lives under `website/assets/media/`. Want me to open a `NEED` list of exactly which files each fragment references?
- QUESTION: 2026-08-10 · @Finn/@Expo — `sections.json` (mine to ratify) still doesn't exist, but you've built 5 per-page hero sections (`careers-hero`, …) rather than the slot-driven shared `hero-page` from SPEC-001/002. That's fine for the Phase-3 parity proof, but before I wire the plugin I need the manifest: Finn, draft `sections.json` with the sections as they actually are (real default copy per the slot-defaults contract) and hand it up. I'll ratify + write v2.1.0 against it.
- DECISION: 2026-08-10 · **SPEC-001 (stacking sections) APPROVED to build, with four amendments.**
  (a) **Invert Phases 2↔3** — harvest ONLY the sections Careers needs first {`hero-page`, `badge-rule`, `perks-list`, `apply-button`, `cta-band`}, prove parity, then harvest the remaining archetypes. Front-loads the contract test the Finesser flagged. (b) **Drop B4** (strip `<style>` on repeat) for now — scoped `.brgw-sec--<id>` CSS makes a duplicate block idempotent; the strip-regex is the same markup-regex failure §8 rejects. If a page ever uses a section twice AND the bytes matter, hoist section CSS to a collected head-inject properly. So plugin change #3 ships WITHOUT the `$seen`/strip-style bit. (c) **Motion parity** confirmed in code (per-`.reveal` stagger, one `.reveal` per section) — Finesser runs the screenshot-diff at the Phase-3 go/no-go with the harness they built. (d) Amendments (a)+(b) are edits to SPEC-001 §7/§3 — **@Explorer please amend your spec** to record them.
- DECISION: 2026-08-10 · **Sequencing gate — nothing modifies the deployed plugin until v2.0.0 is verified live.** Agree with the Finesser: shipping v2.1.0 (or even the vcc_shared_assets refactor) on top of an unverified v2.0.0 baseline means we can't isolate a regression. I may WRITE v2.1.0 in the repo (versioned), but **no upload to `/wp-content/mu-plugins/` until one `[brg_<slug>]` page renders live showing `<!-- vc_embed brg/<slug> v2.0.0 -->` + nav.** Same human step already blocking go-live; buys a known-good baseline for free.
- DECISION: 2026-08-10 · **Working tree — pathspec staging (option a), not worktrees.** Ownership is directory-disjoint, so explicit per-area pathspecs fix the demonstrated root cause (`git add notes/` sweeping in-flight files) while keeping the shared-`main` "pull every turn sees everyone" model intact. Codified in MANIFESTO §Working protocol. Worktrees stay as an opt-in escape hatch for big sweeps (Phase 4).
- DECISION: 2026-08-10 · **SPEC-001 §9 answers.** Q1 slots: **yes but few** — `heading`/`sub`/`cta_label`+`cta_href`/`anchor` only; body/lists/grids stay in git. **Adopting the Finesser's refinement as a contract rule:** since the compose harness can't see WP-stored slot values, **`sections.json` defaults must be the real production copy, not placeholders**, so a default compose-test represents the live page. Q2 `[brg_footer]` **explicit** — approved. Q3 cold-cache — **cap ~8 sections/page, section TTL 600s** (ttl=0/`?brg_refresh=1` still bust during builds). Q4 `brgw-sec`/`brgw-sec--*` — **added to MANIFESTO §Shared tokens** this turn.
- DONE: 2026-08-10 · MANIFESTO edits: Q4 section tokens added; ownership table extended (`website/sections.json`→Controller, `website/sections/<id>/embed.html`→Finesser); staging-discipline rule with per-role pathspecs.
- PLAN: 2026-08-10 · **Assignments.** @Finesser — best unblocked turn is to **front-load the Phase-3 contract proof locally** (not the content gaps, which are still content-blocked): harvest the 5 Careers sections into `website/sections/<id>/embed.html` per the §4 contract (scoped CSS, root `reveal`, no `.brgw` wrapper), extend your harness to assemble a stacked Careers from those section files, and screenshot-diff it vs monolith `[brg_careers]` desktop+mobile. Draft the `sections.json` (with **real** default copy) in your workspace and hand it up as a `NEED:` — I own/ratify the real `website/sections.json`. If local parity holds, I write plugin v2.1.0 immediately (deploy still gated on v2.0.0 live-verify). @Explorer — amend SPEC-001 §7/§3 per the phase-inversion + B4-deferral above.
- DONE: 2026-08-09 · Added `website/wp-snippets/brg-password-gate.php` — BRG-branded WP password gate (Oxygen-safe auto-gate + `[brg_password]` shortcode), reskinned from the Temper gate. Native WP postpass, independent of the embed plugin. Install = WPCode PHP snippet "Run Everywhere" (or a mu-plugin file). Lock via Pages → Quick Edit → Password.
- DECISION: 2026-08-09 · Adopted the Explorer/Finesser/Controller model (from the TEMPER handoff), adapted for BRG. This chat is the Controller. Finesser owns page fragments + shared `brgw.css`/`brgw.js`; Explorer proposes sections/features as specs. Shared-asset / plugin / `pages.json` changes clear through me before landing. See `MANIFESTO.md`.
- DONE: 2026-08-09 · Stood up `MANIFESTO.md`, `STATUS.md`, `notes/{manager,finesser,explorer}.md` at repo root.
- DONE: 2026-08-09 · Community give-back converted to auto-advancing slider + reusable `.brgw-slider` engine; doodles hidden < 560px (mobile motion audit). Pushed `f1b3c8e`.
- NEED: 2026-08-09 · Human: create the 5 WP pages and drop `[brg_<slug>]` on each (all `/<slug>/` are 404 today). Blocks go-live + live plugin verification.
- PLAN: 2026-08-09 · Once one WP page exists, verify `<!-- vc_embed brg/<slug> v2.0.0 -->` + nav render live, then greenlight the other four.
