{
  "_note": "Slots for [brg_careers-hero]. Declared beside the fragment so a slot and its {token} ship in one commit. Generated into website/acf/ by `python3 kit/build-acf.py`; never hand-edit an acf.json. Slot names are underscore-only. Defaults are esc_html'd, so write literal characters, never `&amp;` or `&mdash;`.\\n\\nTHE <h1> IS WIRED. Approved editable by Sean 2026-08-13. LENGTH IS A HINT, NOT A LIMIT — under ~50 characters keeps it to 3 lines at 1440, but character count is only a proxy because WORD length drives the break: careers-hero renders 4 lines at 42 characters where community-hero renders 3 at 43, same rule and same width. 4 lines is the practical ceiling; at ~70 characters the headline is 5 lines and consumes the whole fold, pushing the next section off-screen. Measured, not guessed. The hero doodles were pinned to the padding band first (b324b35) so a longer headline can no longer drag one into the type — before that fix the top mark fell 43px between a 3-line and a 5-line headline and ended up level with the first line. NOTE this headline STARTS a line closer to the ceiling than the other three — it is already 4 lines at its current 42 characters, so it has roughly one line of headroom rather than two.",
  "heading": {
    "type": "text",
    "label": "Headline",
    "doc": "The hero display headline. Approved editable by Sean 2026-08-13. LENGTH IS A HINT, NOT A LIMIT — under ~50 characters keeps it to 3 lines at 1440, but character count is only a proxy because WORD length drives the break: careers-hero renders 4 lines at 42 characters where community-hero renders 3 at 43, same rule and same width. 4 lines is the practical ceiling; at ~70 characters the headline is 5 lines and consumes the whole fold, pushing the next section off-screen. Measured, not guessed. The hero doodles were pinned to the padding band first (b324b35) so a longer headline can no longer drag one into the type — before that fix the top mark fell 43px between a 3-line and a 5-line headline and ended up level with the first line. NOTE this headline STARTS a line closer to the ceiling than the other three — it is already 4 lines at its current 42 characters, so it has roughly one line of headroom rather than two.",
    "default": "Come work somewhere you actually want to be"
  },
  "sub_1": {
    "type": "textarea",
    "label": "Sub copy — before the highlight",
    "doc": "Everything up to and including the em dash. The space before the highlight is in the markup, so no trailing space here.",
    "default": "We're building careers here, not just filling shifts. If you're driven, hospitality-minded, and ready to grow —"
  },
  "sub_highlight": {
    "type": "text",
    "label": "Sub copy — highlighted phrase",
    "doc": "Sits in the yellow marker block that closes the sentence. Short — it is a highlight, not a clause.",
    "default": "let's talk."
  },
  "hl_variant": {
    "type": "text",
    "label": "Highlight animation",
    "doc": "How the marker highlight behind the sub copy arrives. `h3` (default) rough hand-drawn block + ~5deg angled wipe, closest to the comps. `h1` background wipe — THE ONLY ONE THAT SURVIVES A LINE WRAP; switch to it if you lengthen 'Sub highlight' enough to reflow, because h3's block is one rigid shape that cannot wrap with the text. `h0` static, no animation. Unknown or blank renders a static painted mark, never blank. Catalogue: website/lab/README.md.",
    "default": "h3"
  },
  "uline_variant": {
    "type": "text",
    "label": "Underline animation",
    "doc": "How the hand-drawn underline (line-careers-lg.svg) reveals. `u3` (default) filled brush + ~5deg angled wipe, keeps the taper. `wipe` straight-edged reveal, the conservative fallback. `u1` scaleX bar — this is what this hero used BEFORE the artwork swap, kept so the old look can be compared live. Unknown or blank renders the mark static and visible, never hidden. Catalogue: website/lab/README.md.",
    "default": "u3"
  }
}
