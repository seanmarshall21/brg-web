{
  "_note": "Slots for [brg_community-give]. The `slides` repeater was wired by conti 2026-09-23 at Sean's request — finn owns this file, same arrangement as team-members. Declared beside the fragment so a slot and its {{token}} ship in one commit. Generated into website/acf/ by `python3 kit/build-acf.py`; never hand-edit an acf.json. `text`/`textarea` defaults are esc_html'd on fill, so write literal characters — a literal `&` and a literal `—`, never `&amp;` or `&mdash;`.\\n\\nTHE LEDE IS THREE SLOTS, NOT ONE, because it wraps an inline <b> and no field may carry HTML. The spaces either side of the <b> live in the markup on purpose: ACF trims on save, so a default ending in a space would lose it and the words would run together.",
  "lede_1": {
    "type": "textarea",
    "label": "Lede — before the emphasis",
    "doc": "Opening clause, ending at the em dash. The space before the emphasised phrase is in the markup, so do not add a trailing space here. Write the em dash as a literal —.",
    "default": "At BRG, community isn't a marketing initiative —"
  },
  "lede_emphasis": {
    "type": "text",
    "label": "Lede — emphasised phrase",
    "doc": "Rendered in the display face, slightly larger, mid-sentence. Short — a clause, not a sentence.",
    "default": "it's in our DNA."
  },
  "lede_2": {
    "type": "textarea",
    "label": "Lede — after the emphasis",
    "doc": "The rest of the paragraph. The space after the emphasised phrase is in the markup, so start straight in with a capital.",
    "default": "When our restaurants thrive, the people around us should feel that too. That means showing up at local events, supporting causes that matter, partnering with schools and nonprofits, and investing in the places our guests call home."
  },
  "banner": {
    "type": "text",
    "label": "Teal banner",
    "doc": "The rotated teal band straddling the white intro and the dark slider. Sized to its content (width:max-content, capped at 92%) so length is forgiving, but it renders at up to 3rem — past ~28 characters it starts wrapping and the rotation reads badly.",
    "default": "How we give back"
  },
  "slides": {
    "type": "repeater",
    "label": "Slides",
    "button": "Add slide",
    "doc": "One row per slide, in the order they appear. Drag to reorder. Any number works: the slider builds its own dots, and the panel colour alternates black / yellow by position, so adding or removing a slide re-colours the rest automatically — do not try to set a colour per slide. Until the first row is added the page keeps the three slides it shows today.",
    "sub": {
      "photo": {
        "type": "image",
        "label": "Photo",
        "doc": "Fills the image half of the slide, cropped to fit, so the exact size does not matter. Portrait or landscape both work. A tall crop around 1200px on the long edge is plenty — bigger files only slow the page."
      },
      "title": {
        "type": "text",
        "label": "Title",
        "doc": "Shown in the display face, uppercase. Short — around 24 characters keeps it on one line."
      },
      "body": {
        "type": "textarea",
        "label": "Text",
        "doc": "The paragraph under the title, capped at 44 characters wide. Around 200 characters sits best; much longer and the panel grows taller than the photo beside it."
      },
      "cta_label": {
        "type": "text",
        "label": "Button label",
        "doc": "Optional. Leave blank and no button appears. Two or three words, e.g. 'Apply for a donation'."
      },
      "cta_href": {
        "type": "url",
        "label": "Button link",
        "doc": "Where the button goes — a full https:// address, or a path on this site like /contact/. Ignored when the label is blank."
      }
    }
  }
}
