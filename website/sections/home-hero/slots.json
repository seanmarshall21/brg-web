{
  "_note": "Slots for [brg_home-hero]. Declared beside the fragment so a slot and its {token} ship in one commit. Generated into website/acf/ by `python3 kit/build-acf.py`; never hand-edit an acf.json. Slot names are underscore-only. `text`/`textarea`/`lines` defaults are esc_html'd on fill, so write literal characters — a literal `&` and a literal `—`, never `&amp;` or `&mdash;`.\\n\\nTHE <h1> IS NOW WIRED, as type `lines`. It was declined on 2026-08-13 for one reason only: the headline carries a structural break — `We're in the business / of making your day great.` — and no slot type could carry it. text/textarea escaped it into a visible &lt;br&gt; and html returned <p>-wrapped content nested inside the <h1>. `lines` escapes first and turns the editor's newline into <br> after, which expresses the break without the field accepting markup. The constraint was real; it stopped being a constraint when the type existed.",
  "heading": {
    "type": "lines",
    "label": "Headline",
    "doc": "The hero headline. PRESS RETURN where you want the line to break — it breaks exactly there. Two lines is the design; three starts eating the fold on a phone.",
    "default": "We're in the business\nof making your day great."
  },
  "sub": {
    "type": "lines",
    "label": "Sub copy",
    "doc": "Sits under the marker underline, capped at 52ch. Plain text; markup is escaped. Write `&` and `—` as literal characters. PRESS RETURN to force a line break exactly where you want one.",
    "default": "From a classic Board & Brew sandwich to a slice at Odie's — we're here for the moments that matter."
  },
  "tagline": {
    "type": "text",
    "label": "Yellow tagline",
    "doc": "The short line beneath the sub, in the display face. White, not yellow, per the comp. ~40 characters.",
    "default": "Good food. Real people. Rad experience."
  },
  "uline_variant": {
    "type": "text",
    "label": "Underline animation",
    "doc": "How the hand-drawn underline reveals. `u3` (default) filled brush + ~5deg angled wipe — keeps the taper. `wipe` straight-edged reveal, the conservative fallback with no skewed edge to overhang. `u1` scaleX bar, the lab's baseline, kept so the taper argument can be re-checked live. Unknown or blank renders the mark STATIC and visible, never hidden. u2/u4 (dasharray draws) need the artwork inline as SVG and are not available on an <img>. Catalogue: website/lab/README.md.",
    "default": "u3"
  }
}
